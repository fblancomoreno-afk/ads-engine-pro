# ============================================================
# .env — Variables de entorno Ads Engine Pro
# ============================================================
# IMPORTANTE: Este archivo NO se sube a Vercel como archivo.
# Cada variable se pega una a una en el panel de Vercel.
# Día de producción: yo te guío variable por variable.
# ============================================================

# Base de datos Neon (PostgreSQL)
# Se obtiene en: console.neon.tech → tu proyecto → Connection string
DATABASE_URL=postgresql://usuario:contraseña@host.neon.tech/neondb?sslmode=require

# JWT — clave secreta para los tokens de sesión
# Pon cualquier texto largo y aleatorio, mínimo 32 caracteres
# Ejemplo: una frase sin espacios como "MiClaveSecreta2025AdsEnginePro"
JWT_SECRET=pon-aqui-tu-clave-secreta-larga

# Anthropic — API key de Claude
# Se obtiene en: console.anthropic.com → API Keys → Create Key
ANTHROPIC_API_KEY=sk-ant-api03-...

# Lemon Squeezy — se obtienen al crear los productos
# Se obtienen en: app.lemonsqueezy.com → Settings → Webhooks
LEMON_WEBHOOK_SECRET=tu-webhook-secret-de-lemon

# IDs de productos en Lemon Squeezy (uno por plan)
# Se obtienen en: app.lemonsqueezy.com → Products → cada producto → ID
LEMON_PRODUCT_STARTER=123456
LEMON_PRODUCT_PRO=123457
LEMON_PRODUCT_AGENCY=123458

# Puerto local para desarrollo (Vercel lo ignora)
PORT=3000
